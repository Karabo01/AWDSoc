# Alert fixture corpus

One file per decoder family. These are **representative, not captured** — they
were written to the documented shape of each decoder rather than pulled from a
live manager. Replace each one with a real capture as soon as that decoder family
appears on a client's manager, and keep the assertions.

DESIGN.md §13: every mapping bug found in production becomes a fixture here.
